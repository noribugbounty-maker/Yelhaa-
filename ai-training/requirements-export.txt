# Dépendances de la conversion GGUF (export.py), en plus de requirements.txt.
#
#   git clone --depth 1 https://github.com/ggml-org/llama.cpp ai-training/tools/llama.cpp
#   pip install -r ai-training/requirements-export.txt
#
# `convert_hf_to_gguf.py` vit dans le dépôt llama.cpp ; il n'est pas publié sur PyPI.
# En cas d'incompatibilité, préférer le fichier du dépôt cloné :
#   pip install -r ai-training/tools/llama.cpp/requirements/requirements-convert_hf_to_gguf.txt

gguf>=0.10
sentencepiece>=0.2
protobuf>=4
numpy>=1.26
